apiVersion: apps/v1
kind: Deployment
metadata:
  name: app4-nginx-deployment
  labels:
    app: app4-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app4-nginx
  template:
    metadata:
      labels:
        app: app4-nginx
    spec:
      containers:
        - name: app4-nginx
          image: stacksimplify/kubenginx:1.0.0
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: app4-nginx-nodeport-service
  labels:
    app: app4-nginx
  annotations:
#Important Note:  Need to add health check path annotations in service level if we are planning to use multiple targets in a load balancer    
#    alb.ingress.kubernetes.io/healthcheck-path: /index.html
spec:
  type: NodePort
  selector:
    app: app4-nginx
  ports:
    - port: 80
      targetPort: 80